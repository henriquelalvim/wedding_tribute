# ---------------------------------------------------------------------------
# frontend/.env  —  everything here is PUBLIC.
# Vite bakes these values into the JavaScript bundle you publish, so only ever
# put public data here. A private key would be readable by anyone.
# ---------------------------------------------------------------------------

# VITE_CHAIN_ID and VITE_RPC_URL must point at the SAME network — change them
# together, never one without the other:
#   31337 + http://127.0.0.1:8545   (local Hardhat node — the default below)
#   84532 + https://sepolia.base.org (Base Sepolia)
#   8453  + https://mainnet.base.org (Base mainnet)
VITE_CHAIN_ID=31337

# Printed by contracts/scripts/deploy.js
VITE_CONTRACT_ADDRESS=

# Used for the read-only view, so guests with no wallet still see the page.
VITE_RPC_URL=http://127.0.0.1:8545

# "/" for a custom domain, "/<repo-name>/" for a GitHub Pages project site.
VITE_BASE_PATH=/

# Optional. Enables "Entrar com Google" (Privy) as an alternative to MetaMask, with
# gas-sponsored tributes via a smart wallet. Blank hides the button entirely — the
# page works fine with MetaMask only. Get one at https://dashboard.privy.io.
VITE_PRIVY_APP_ID=

# Documentation only — the frontend bundle does not read this. Once you have an app
# id above, paste your Paymaster URL (e.g. from the Coinbase Developer Platform) into
# the Privy Dashboard under Smart Wallets → <network> → Paymaster URL instead.
VITE_CDP_PAYMASTER_URL=
